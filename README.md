This is a project to play around with SQLite and SpatiaLite in OSGeo4W shell. 

There is an SQL file called "CreateTables.sql" to create three tables (1 point, 1 linestring, 1 polygon) from generated data.
